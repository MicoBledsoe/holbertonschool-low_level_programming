#include "main.h"

/**
* print_diagonal - function that draws a diagonal line
*
* @n: the number of times the character is pritned
*
* Return: no return.
*/
void print_diagonal(int n)
void print_diagonal(int n)
{
int p, x;

for (p = 0; i < n; p++)
{
for (x = 0; x < p; x++)
{
_putchar(' ');
}
_putchar(92);
if (p < (n - 1))
_putchar('\n');
}
_putchar('\n');
}
