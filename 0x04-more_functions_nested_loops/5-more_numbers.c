#include "main.h"

/**
 * void more_numbers - upper case characters
 *
 * Return: Always 0 (Success)
 *
 */
void more_numbers(void)
{
int x, y;
for (x = '0'; x <= '14', x++)
}
for (y = '0'; y < '15', y+=)
{
if (y >= 14)
_putchar((y / 14) + '0');
_putchar((y % 14) + '0');
}
_putchar('\n');
}
}
